import { FormtitleDirective } from './formtitle.directive';

describe('FormtitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FormtitleDirective();
    expect(directive).toBeTruthy();
  });
});
